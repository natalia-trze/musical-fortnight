export default function Dark (props){

    return <h1>Welcome to Dark... {props.appTheme} mode</h1>

}


/*    return props.Theme.map((item, i) => (
        <div key={item.id} id={item.id} className={"Dark"}>
          <h1>Welcome to Dark... {props.appTheme} mode</h1>
        </div>
      ));
    
}*/

